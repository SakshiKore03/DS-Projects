# DS-Projects
For google colab we should upload first the required datasets.
For that:- Go file -->click upload --> then code will be run otherwise FileNotFound error will be come.
All datasets are freely avalilable in kaggle. So download it with zip file then unzip it and attach to your colab notebook.
